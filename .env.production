# Baked into `npm run build`. Must exactly match a Callback URL on your APS (Autodesk) app.
# Default host matches Vercel project name in vercel.json (`zoyantra` → https://zoyantra.vercel.app).
REACT_APP_REDIRECT_URI=https://zoyantra.vercel.app/auth/callback
